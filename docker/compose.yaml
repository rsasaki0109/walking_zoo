services:
  locomotion_ros2_jazzy:
    build:
      context: ..
      dockerfile: docker/Dockerfile.jazzy
    command: bash -lc "source /opt/ros/jazzy/setup.bash && source install/setup.bash && ros2 launch locomotion_ros2_bringup mock_runtime.launch.py"
