# Semantic Action Demo

This example area tracks future VLA-facing demos using
`locomotion_ros2_msgs/msg/SemanticAction` and `locomotion_ros2_vla`.

v0.1 includes only the lightweight semantic action mapper library.
