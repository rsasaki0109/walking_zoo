<root BTCPP_format="4" main_tree_to_execute="WalkingZooRecovery">
  <!--
    Recovery tree for a walking_zoo robot. A host populates the `walking_state`
    blackboard entry from /walking_zoo/state and sets `clear_succeeded` from the
    result of a /walking_zoo/clear_fault service call before each evaluation.

    If the robot is already ready, succeed immediately. Otherwise attempt to
    clear the fault and re-check readiness on the refreshed state.
  -->
  <BehaviorTree ID="WalkingZooRecovery">
    <Fallback name="walking_zoo_recovery">
      <CheckWalkingReady walking_state="{walking_state}"/>
      <Sequence name="clear_and_recheck">
        <ClearWalkingFault clear_succeeded="{clear_succeeded}"/>
        <CheckWalkingReady walking_state="{walking_state}"/>
      </Sequence>
    </Fallback>
  </BehaviorTree>
</root>
